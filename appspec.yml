version: 0.0
os: linux
files:
 - source: .
   destination: /www/var/
hooks:
  AfterInstall:
   - location: scripts/install_node
     timeout: 300
     runas: root
  ApplicationStart:
   - location: scripts/start_server
     timeout: 300
     runas: root